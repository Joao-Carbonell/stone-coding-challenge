services:
  kpi-automation-api:
    container_name: kpi-automation-api
    image: pedra-pagamentos/kpi-automation-api:latest
    build: .
    ports:
      - "8000:8000"
    environment:
      - SQLALCHEMY_DATABASE_URI=postgresql://postgres:hE-iAf3L-s%2Bz7%5Bx%4@035.224.238.133:5432/kpi-api-database

      - API_TITLE=KPI API
      - FLASK_DEBUG=0
      - API_VERSION=v3

      - OPENAPI_VERSION=3.1.1
      - OPENAPI_URL_PREFIX=/
      - OPENAPI_SWAGGER_UI_PATH=/docs
      - OPENAPI_SWAGGER_UI_URL=https://cdn.jsdelivr.net/npm/swagger-ui-dist/
      - OPENAPI_REDOC_PATH=/redoc
      - OPENAPI_REDOC_UI_URL=https://cdn.redoc.ly/redoc/latest/bundles/redoc.standalone.js
      - PORT=8000

      - JWT_SECRET_KEY=secret_key
      - SECRET_KEY=secret_key
      - JWT_TOKEN_LOCATION=headers
    depends_on:
          - kpi-database
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

